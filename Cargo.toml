[package]
name = "rustcask"
version = "0.1.0"
edition = "2024"

[dependencies]
dotenv = "0.15"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

[features]
mock = []
